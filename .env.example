# Default: no backend or external account required.
VITE_DATA_SOURCE=local

# Enable http only after implementing docs/connectors.md.
VITE_API_BASE_URL=/api

# VITE_* variables are public browser configuration. Never put secret keys here.
